name: Output Opened Issue
on:
  issues:
    types:
      - opened
jobs:
  output:
    runs-on: ubuntu-latest
    steps:
      - name: Output Issue Context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
